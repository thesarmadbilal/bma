
{
  "name": "bma-react-theme",
  "version": "1.0.0",
  "description": "BMA Capital React WordPress Theme",
  "scripts": {
    "build-copy": "npm run build && node copy-build.js",
    "build": "cd ../../ && npm run build",
    "zip": "npm run build-copy && node create-zip.js",
    "clean": "rimraf build/* ../bma-react-theme.zip",
    "dev": "cd ../../ && npm run dev"
  },
  "devDependencies": {
    "fs-extra": "^11.1.1",
    "archiver": "^5.3.1",
    "rimraf": "^5.0.1"
  }
}
